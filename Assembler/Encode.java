public class Encode{
  public String[] data;
  public Encode(Input in){
    String[] input = in.file;
  }
  
  private void multiplex(String[] input){
    String[] output = new String[input.length];
    for( int x = 0; x < input.length; X++){
      switch ( input[x].charAt(0) ){
        case '@' : output[x] =  
      }
    }
  }
  
  private String aCase(String input){
    
  }
}